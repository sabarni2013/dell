import { CalculateDirective } from './calculate.directive';

describe('CalculateDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculateDirective();
    expect(directive).toBeTruthy();
  });
});
